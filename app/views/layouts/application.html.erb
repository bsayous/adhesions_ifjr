<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "AdhesionsIfjr" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

  </head>
  <body>

    <div class="navbar navbar-inverse navbar-fluid-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <a class="brand" href="#">AdhesionsIfjr</a>
          <div class="container-fluid nav-collapse">
            <ul class="nav">
              <% if compte_signed_in? %>
                <li>
                  <%= link_to comptes_mes_infos_path do %>
                    <i class="icon-user"></i> Mes infos
                  <% end %>
                </li>
                <li class="dropdown">
                  <a href="#" role="button" class="dropdown-toggle" data-toggle="dropdown">
                    <i class="icon-cogs"></i> Administration
                    <i class="icon-caret-down"></i>
                  </a>
                  <ul class="dropdown-menu" role="menu">
                    <li class="nav-header">Personnes</li>
                    <li>
                      <%= link_to individus_path do %>
                        <i class="icon-group"></i> Individus
                      <% end %>
                    </li>
                    <li>
                      <%= link_to structures_path do %>
                        <i class="icon-th-large"></i> Structures
                      <% end %>
                    </li> 
                    <li class="nav-header">Adhésions</li>
                    <li>
                      <%= link_to periode_adhesions_path do %>
                        <i class="icon-calendar"></i> Périodes
                      <% end %>
                    </li> 

                  </ul>
                </li>
              <% end %>
            </ul>
            <ul class="nav pull-right">
              <% if compte_signed_in? %>
                <li>
                  <%= link_to destroy_compte_session_path, :method => :delete do %>
                    <i class="icon-signout"></i> Se déconnecter
                  <% end %>
                </li>
              <% end %>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container-fluid">
      <div class="row-fluid">
        <div class="span12">
          <%= bootstrap_flash %>
          <%= yield %>
        </div>
      </div><!--/row-->

      <footer>
        <p>&copy; Company 2013</p>
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
